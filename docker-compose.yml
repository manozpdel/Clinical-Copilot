services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: clinical-copilot:latest
    container_name: clinical-copilot-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped