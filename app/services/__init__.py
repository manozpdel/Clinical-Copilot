"""Business logic services for the Clinical Copilot REST API.

This package contains ONLY service classes that orchestrate calls into
existing business logic (the LangGraph agent, the voice pipeline, and
evaluation utilities from earlier milestones). It contains no FastAPI
routing or request/response validation.
"""
